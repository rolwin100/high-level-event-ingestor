# Database (PostgreSQL from docker-compose)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/event_ingestion

# Redis (from docker-compose)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=  # optional for local dev

# App
PORT=3000
